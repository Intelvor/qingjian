# 更新日志

一个版本一节，标题写「版本 · 日期 · 渠道」（渠道是 alpha / beta / rc / stable），正文一行一条、面向用户的措辞。
发版时 CI 从这里取该版本的说明写进 GitHub Release，并汇总成官网下载页用的 `releases.json`（`tools/release/releases_json.py`）。

## 1.0.9 · 未发布 · beta

- Windows：关掉「英文模式给候选」（或某个应用在 `[apps] english_candidates_off` 名单里）后，英文模式变成**纯直通**：一个键都不吃，字母 / 数字 / 标点原样交给应用 —— 打游戏、用快捷键、在自绘控件里打字都不会再被输入法吞掉；中 / 英切换键照常（单击 `Shift` 一样能切回中文）。以前这条只在候选上「不给」，按键仍被输入法吃掉再自己插一遍

## 1.0.8 · 2026-09-18 · beta

- 云联想改成「发原始输入、回什么用什么」：发给 AI 的只有**你敲的原始按键**（本机不再切分、纠错）、光标前后文、候选窗口第一页，外加当前输入方式（全拼 / 双拼 / 注音 / 五笔）与简繁设置；返回的词与整句**本机不再拿拼音校验**，直接进候选（只丢掉与候选窗口第一页重复的）。夹着英文词的句子也能补了：敲 `javashiyimenbianchengyuyan` 能补出「Java 是一门编程语言」（以前这种拼音切不开的输入会被整片挡掉）
- 云联想：敲到 20 个字母后不再要云端词，只留整句补全 —— 这么长的输入本来就是一整句话
- Windows：在中文模式下原样上屏过的一串字母（例如 `javashiyimenbianchengyuyan`）以前会被记成「个人英文词」，却只躺在 `user-english.tsv` 里、界面上找不到；现在「词库 → 个人词」把个人英文词一并列出来（标注「上屏过 N 次」），删除时中文个人词与个人英文词一起清掉
- Windows：词库页不再列出 `._animals.qj` 这类 macOS 元数据文件（163 字节的 AppleDouble，随产品数据混进来的）：它们后缀也是 `.qj`，以前会显示成「读不了：文件损坏或格式不对」，看着像词库坏了；装机脚本也会在升级时把已经装进去的清掉

## 1.0.7 · 2026-09-18 · beta

- 新增**五笔（86 版）**：设置「通用」页多了「五笔（86 版）」开关，与「拼音方案」各自一条轴——两个都开着就是**混输**（五笔候选在前，打不出的字直接打拼音），只用五笔就把拼音方案选「关（只用形码）」。随包极点 86 码表（Apache-2.0），译词、生词记录与学习照常
- 输入方案改成「拼音方案」一个下拉（全拼 / 五套双拼 / 大千注音 / 关）：原来的「双拼方案」与「大千注音」开关合并进来，老配置里的 `shuangpin` / `zhuyin` 键照旧认
- Windows：小键盘的数字与运算符现在能进算式模式（`v`），小键盘敲的 `.` 也始终半角——#150 把这些键收进输入法之后带出来的两个毛病
- Windows：开着设置窗口把「内置英文模式」关掉，任务栏的中 / 英按钮立刻消失（以前要切走再切回输入法才跟上）
- Windows：选过 `OK` / `PC` 这类两字母全大写缩写之后，再敲 `ok` / `pc` 它们会排在中文前面（以前这条规则不看学习记录，一刀切让中文先）
- 去掉写死的 `mp3` / `mp4` 快捷候选：现在是「回车出字母再敲数字」那条通用路子，少两条没人要的特例
- Windows：安装向导里的中文说明不再乱码（`.iss` 补回 UTF-8 BOM）；按应用设置里以 `*` 结尾的条目匹配多字节应用名时不再可能崩
- Windows：「输入行为」页新增「新窗口的输入模式」：可选记住上次（默认）、中文、英文，窗口第一次激活时按它设一次；顺带修好「设了默认中文，新开的窗口还是英文」——系统会把上次的模式写回来盖掉我们设的，现在激活后那一瞬的写回会被忽略
- Windows：「词库」页新增「个人词」：列出你选过、云端接过来的词，可以逐个删掉（删掉后青简立刻忘掉它，含它的个人 n-gram 痕迹）；词多了分页翻（每页 50 条），也可以用筛选框按词或拼音找
- Windows：任务栏的输入模式图标也改成方案字母：全拼「拼」、双拼「双」、大千注音「注」、五笔「五」（与拼音同时开着时出「五」）、英文模式「英」、Caps Lock 亮着「A」——一格 16px 位图只画一个字，与悬浮状态条那格同一套口径
- Windows：悬浮状态条的模式格改成**方案字母**：全拼 `拼`、双拼 `双`、大千注音 `注`，开着五笔再跟一个 `五`（`拼五` = 拼音 + 五笔混输）；英文模式 `英`，Caps Lock 亮着时这一格只显示 `A`。切中 / 英、切全 / 半角、Caps 亮灭都不改变整条的长度
- Windows：放进 `%APPDATA%\Qingjian\dicts` 的词库现在真的会加载——以前只认 `.qj` 与 `.tsv`，Rime 的 `.dict.yaml` 与 `.txt` 放进去等于没放，界面上还一个字都不显示
- Windows：修好「续写」看不到你正在写的内容：上下文以前读的是上一段结束时提交的那份旧文本，现在读的是光标前后的真实内容
- Windows：设置窗口补上标题栏左上角的图标（以前是系统的空方框）；「使用说明」页的状态条一节补上「☁」格、Caps 的「A」与注音时的「注」

## 1.0.6 · 2026-09-17 · beta

- Windows：「候选窗口」页新增「主题色」：青简绿（默认）/ 经典蓝。它同时管候选窗的高亮与悬停底色，以及悬浮状态条上「中 / 英」「，。」和云朵的颜色——以前换的只是候选窗那一处，状态条不跟着变
- Windows：任务栏上的中 / 英图标换成新设计稿：按屏幕缩放挑清晰档位，浅色任务栏上不再是白字看不清；Caps Lock 亮着时显示「A」
- Windows：「通用」页的输入方式里新增「小浪双拼」
- 双拼下用 `Shift` + `V` / `Shift` + `U` / `Shift` + `I` 进表达式 / 问字 / 续写：`v` `u` `i` 在双拼里都是音节键。顺带修好续写：双拼下 `Shift` + `I` 以前被当成普通大写字母交给应用，续写进不去
- 算式模式（`v`）的数字多出小数与金额写法：`v123.5` 出一百二十三点五 / 壹佰贰拾叁点伍 / 一百二十三元五角 / 壹佰贰拾叁元伍角，`v123` 多出「一百二十三元整 / 壹佰贰拾叁元整」
- Windows：英文模式的选词与中文一致：空格选高亮那个、数字选第 N 个、翻页键翻页（以前空格要先按方向键动过高亮才选得中）
- Windows：英文直输段修好两处：`gpt-6` 不再丢 6；空格上屏时空格也保留
- Windows：当前页没有第 N 个候选时，这个数字不再被吞掉：中文模式接进英文直输段、英文模式接进单词（`gpt6` 出 `gpt6`）
- 拼写纠错更宽容：相邻两键敲反后不用等整个词敲完就能纠（`mignt` 出「明天」）
- 修复上屏对齐的两处：敲错变体的词上屏后不再残留拼音（`pingyin` 选「拼音」不再剩 `gyin`）；没打完的音节不再被记成一次假敲错
- Windows：新账户第一次打开设置程序会自己建好配置目录并写出模板（以前保存失败、「在编辑器中打开配置文件」指向不存在的文件）
- Windows：「使用说明」页补上「英文模式」一节，前缀键与算式模式的说明跟上现在的手感：双拼下按 `Shift` 显示，注音下说明这三条前缀被键盘布局占用
- Windows：竖排候选窗有最小宽度（候选很短时不再窄得挤在一起）；设置页标签列收窄、双拼那一项改叫「输入方式」

## 1.0.5 · 2026-09-17 · beta

- Windows：悬浮状态条的「中 / 英」改成跟着**前台应用**走。以前切到别的应用后它不动、有时还来回乱跳，现在切到哪个应用就显示那个应用的中 / 英；前台应用没在用青简时（切成了别的输入法，或焦点落在没有输入框的地方）收起，切回即恢复
- Windows：修复多个应用同时开着时的「串味」：按应用的设置（比如「终端 / 编辑器中不给英文候选」）偶尔会套到别的应用上，两个应用的中 / 英也会互相覆盖
- Windows：修复点状态条「中 / 英」可能切到别的应用——以前是所有装了青简的应用谁先来取就归谁，现在只有前台那个应用会切
- Windows：修复状态条的鼠标判定区域比看得见的外观大一圈（阴影那一圈几乎看不见的地方以前也挡着点击）

## 1.0.4 · 2026-09-17 · beta

- Windows：「候选窗口」页新增「字体大小」：候选文字、序号与释义按 8–32 点可调，默认 16 点；候选窗和悬浮状态条一起变，改完即生效
- Windows：修复点状态条「中 / 英」切换有时没反应——以前在输入法没有前台焦点（比如刚切走应用）时点了不生效，现在切成什么就是什么，立刻生效
- Windows：设置「关于」的图标因为是问号不大直观，改成了标准的「ⓘ」信息图标

## 1.0.3 · 2026-09-17 · beta

- 云联想新增「续写」：没在打拼音时敲一下 `i` 再按 `Tab`，云端只看光标前后的文字、接着往下写一小段（`i` 本身不上屏），再按一次 `Tab` 采用。前缀键现在是三个：`v` 算式、`u` 问字、`i` 续写，在「快捷键」页改
- Windows：设置新增「使用说明」页：打字、中英切换、前缀键、译词与删候选、云联想、注音、悬浮状态条的按键速查；**显示的按键跟着你的设置变**（改了翻页键、译词键、中英切换键，这里立刻跟着换）
- Windows：「通用」页新增「繁体输出」：上屏的是繁体，词库、词频与学习记录仍按简体走
- Windows：注音模式（大千）的按键按键盘布局来：数字键与 `- ; , . /` 都进组句当声调与符号、不再选词，选词用 `Enter`，注音符号原样上屏用 `Shift + Enter`，`Space` 是一声
- Windows：修复横排候选的点击范围：以前点候选下方的译文行或空白也会把那个词选上，现在只在候选那一块里才算
- Windows：「候选窗口」页的「系统绘制」标注为「（不推荐）」——它是渲染器出问题时的退路

## 1.0.2 · 2026-09-17 · beta

- Windows：「云服务」页新增「前文长度」与「后文长度」两个数字框：发给云端的上下文上限（字），缺省 64 / 32，0 表示那一侧不发。原来只能在配置文件里改，界面改完立刻就生效（TSF 侧按上限读光标前后的文本，真正发多少由这个值裁剪）
- Windows：设置里新增「输入行为」页：中英切换键、Shift + 字母、中英文标点全角、英文模式候选（含「终端 / 编辑器中不给」）、中文候选排在英文词前面都从「通用」页搬了过去；「通用」页只剩学习语言、每页候选数、双拼与大千注音
- Windows：「关于」页说明这是上游青简的 Windows 增强版；原来那两个按钮改标为「上游官网」「上游 GitHub」，另加一个「本版仓库」指向本改版自己的仓库
- Windows：中英切换键不再提供「Ctrl + Space」这一项（1.0.0 加过）：那个组合与系统的「输入法/非输入法切换」抢得厉害，体验不成熟。现在只剩单击 Shift、单击 Ctrl、不切换三个选项；1.0.0 里配过 Ctrl + Space 的，这次升级后会退回缺省的单击 Shift

## 1.0.1 · 2026-09-17 · beta

- 修复：1.0.0 里「整句补全会带上光标前后的上下文」那条实际没生效（改动在打包之前被漏掉了），云端整句看不到你在写什么，只能按拼音硬猜——表现是补出来的句子常常接不上话题、或者因为对不上拼音而被丢掉。这一版把上下文真正接上了

## 1.0.0 · 2026-09-17 · beta

- 这是[青简](https://github.com/qingjian-team/qingjian)的 **Windows 增强版**：基于上游 0.1.3（尚未发布），Windows 这一路补了一批交互与设置项。版本号从 1.0.0 起、与上游的 0.1.x 不同号，免得两边的包在下载页上撞车
- Windows：候选窗能用鼠标：候选行点选上屏、悬停浅底色，整句补全那块也能点（命中矩形由渲染器给出，看得见高亮的范围就是点得着的范围）
- Windows：整句联想的时机可配：「设置 → 云服务」的「整句联想时机」选「按 Tab 键时才联想」，停止输入不再自动请整句，按一下 Tab 才开始算
- Windows：悬浮状态条「☁」格切换在线联想、「⚙」格打开设置；两个图标改成自绘（☁ / ⚙ 这两个码位会被 Segoe UI Emoji 接走画成彩色，不吃主题色）
- Windows：中英切换键可配：「设置 → 通用」选单击 Shift / 单击 Ctrl / Ctrl + Space / 不切换；另可关掉内置英文模式（关掉后固定中文模式，切换键与状态条都不再切过去）
- Windows：中文模式下按住 Shift 敲的字母可选「进组句」：大写按小写参与匹配，Cpan 与 cpan 一样能出「C盘」
- Windows：`-` `=` 由壳自己插入，不再放行：放行的键在 Edge / QQ 这类宿主里到不了应用，用户看到的是「中文模式按 - 没反应」
- Windows：候选词间距松开一档（竖排行距、横排列距各加一点）
- Windows：刚学的用户词第二次就打得出来：学进用户词库的词词频提到 10000，高于词库常见的词频（原来是 100，比词库中位数还低）
- Windows：连不上 Server 时 DLL 自己把它拉起来，但只在普通桌面应用里拉：按宿主进程的完整性级别判断，商店应用、登录界面这类不拉
- 整句补全：光标前后的上下文现在会发给云联想（前最多 64 字、后最多 32 字），模型能接上你正在写的话题；之前 Windows 只把前文发给本地整句模型
- 整句里的数字由模型按中文习惯取舍：金额、数量、序号写阿拉伯数字，口语量词写汉字（敲 `wohuayibaiershisanyuanmailesangempsan` 补出来是「我花123元买了三个mp3」）

## 0.1.3 · 未发布 · beta

- 新增西班牙语学习语言：「设置 → 通用」选西班牙语后候选旁显示西语词性与译词；释义表由机器翻译生成、未经人工校对，错译欢迎反馈（#37、#48、#49）
- `?` 不再自动进入问字：没在组句时敲 ? 就是问号；想保留旧行为到「设置 → 快捷键」勾选（#83）
- 中英混输可选「中文优先」（缺省关）：开着时整段输入是英文词（hello、key）也让中文候选排第一、英文词固定第二，空格上屏的是中文；关着时拼音不成立的输入英文词排第一。「设置 → 通用」勾选（#85）
- 「lüe / nüe」类音节 lue / nue 与 lve / nve 两种写法都能稳定命中（如 celue → 策略），双拼方案尤其受益（#59、#60）
- 输入时的候选查询少做一遍重复计算，响应更快（#64、#65）
- 私密输入（Windows 的密码框与无痕窗口、macOS 的 Secure Input）里的词不再记进词汇记录；多个应用同时在输入时各自的组句与学习互不干扰（#89）
- 候选窗口（Windows 还有悬浮状态条）改由青简自己的渲染器绘制，两平台像素一致，为主题铺路：「设置 → 候选窗口」可选「渲染引擎」与「字体」（系统里装的任一字体）；不合适可切回「系统绘制」
- 云联想接口地址是 OpenCode 时自动带上会话标识（#39）
- 「高级」页新增「学习输入习惯」开关：关掉后不再按你的选择调整候选顺序、不再记新词与敲错纠正；已学过的照常生效，随时可再打开
- 学习语言可选「不显示译文」（配置 `learning_language = "off"`）：候选旁不出译词，生词标记与释义兜底一并停；Windows 换学习语言不再需要重启（#123）
- 中英混杂词直接按「字母 + 拼音」打：cpan → C盘、bzhan → B站、youpan → U盘（与 优盘 并列）、txu → T恤、agu → A股（#93）
- 翻页键可选「- =」：「设置 → 快捷键」翻页键多一项，缺省仍是 [ ]；选它时输入拼音中敲 - 是翻页，不再进英文直输（#43、#116）
- 英文模式选词改成与中文一致：`Space` 选高亮候选、数字键选第 N 个、翻页键翻页；`Enter` 与标点仍原样输出所敲字母。之前空格要先用方向键动过高亮才选词、数字键只当数字
- 双拼下可以用算式与问字了：没在输入拼音时按 `Shift`+`V` 再接算式（`V1+2`）、`Shift`+`U` 再接拼音或码点（`U4e00`），与搜狗、微软双拼一致。之前 v / u 被音节占用，双拼下这两个功能进不去（#11）
- `v` 加数字多出金额写法，填单据、写收据用：`v123.5` 出「一百二十三点五」「壹佰贰拾叁点伍」「一百二十三元五角」「壹佰贰拾叁元伍角」，`v123` 在中文数字之后多出「一百二十三元整」「壹佰贰拾叁元整」
- 修复靠纠错读出的词上屏后拼音行还剩半截的问题：敲 pingyin 选「拼音」，之前会剩下 gyin（#128）
- 数字键对应的候选不存在时不再被吞掉：`gpt6` 只有三个候选时敲 6，数字接在字母后面原样输出（之前 macOS 会把 gpt 上屏并丢掉 6，Windows 没有任何反应）
- 新增「繁体输出」：「设置 → 通用」打开后候选与上屏文字转成繁体（只换字形），学习、译词、云端词照常（#41）
- 应用图标换成新的竹简 logo
- Windows：修复组句中打了 `-` 这类符号之后再敲数字被吞掉的问题（`gpt-6` 只出 `gpt-`）；这种英文直输段按空格上屏时空格也保留，与 macOS 一致
- Windows：修复电脑上第二个账户打不开配置、设置保存不了的问题：这个账户下输入法还没运行过时配置目录不存在，现在设置程序自己会建好并写出配置模板（#10）
- Windows：任务栏的中 / 英图标换成新设计稿，按屏幕缩放选清晰的尺寸，浅色任务栏上不再是白字看不清；Caps Lock 亮着时显示「A」
- Windows：修复 Windows 10 上打不开「青简设置」（报「无法定位程序输入点 TryCreatePackageDependency」）：设置程序自带所需的运行库，不再要求系统装有 Windows App Runtime；系统要求改为 Windows 10 1809 及以上（#77、#18）
- Windows：安装包多带一份 32 位输入法组件：企业微信、WPS、32 位 QQ 这类 32 位应用里之前只能出字母、或被系统换回别的输入法，现在能正常打字（#76、#13）
- Windows：数字与标点按当前系统键盘布局识别：非美式布局（如德语）下以前会按美式键位出错误的符号（#115）
- Windows：修复导入 Rime .dict.yaml 词库后候选里没有词的问题：导入时转成青简格式并自动启用，可一次多选；词库目录里新增、替换、移除的文件一秒内生效，不用重启输入法（#36、#118）
- Windows：修复企业微信、微信、Notepad++ 这类自绘输入框里数字键和英文标点没反应的问题：没在组句时敲的数字、英文模式下敲的标点之前会被吞掉（#25、#26）
- Windows：输入法组件不再依赖 VC 运行库（vcruntime140.dll）：没装运行库、或运行库文件被别的安装程序改坏了权限的机器上，任务栏搜索、开始菜单这类系统应用会因为加载不了青简而悄悄切回上一个输入法，现在不会了
- Windows：新增大千注音输入：「设置 → 通用」打开后按注音键盘敲、候选与拼音一样出，悬浮状态条显示「注」；声调暂不参与筛选。注音模式下数字键与 - ; , . / 都进组句，选词改用 Enter，Shift+Enter 原样上屏，空格是一声（#22、#24、#41）
- Windows：首次启动自动写出带说明的 config.toml，不用先在设置程序里保存一次（#19）
- Windows：附加词库只从用户目录的 dicts\ 装，不再把学习数据文件当词库加载
- Windows：日志集中到一个目录「%LOCALAPPDATA%\Qingjian\logs」：引擎、各应用内的输入法部分、设置程序各一个前缀，按天分文件、保留 7 天；设置程序「关于」与「高级」页新增「打包日志到桌面」，反馈问题发一个 zip 即可
- Windows：设置程序启动失败时会弹出提示并写日志，不再静默退出
- macOS：自定义短语：偏好设置新增「自定义短语」页，给一段输入码（1–32 个字母）指定固定候选位置（1–9），同一输入码可配多条；通用页新增默认中文标点全角 / 半角（#15、#16、#17）
- macOS：修复中文模式用半角标点时，单独敲 ? 再按空格或回车丢掉问号（#21）
- macOS：系统设置「键盘 → 文本替换」里的条目在输入拼音时也可用，输入码敲全后短语占该码最靠前的空位；「自定义短语」页可关（#84）
- **已知问题（Windows）：本版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡。腾讯软件管理这类带反注入保护的安全软件内无法输入，是其自身机制，除微软拼音外的输入法均如此**
- 本版社区贡献：@Shiyuhai2023（Windows 10 设置程序）、@pinchiu（注音、繁体输出）、@Tofuzhu（西班牙语）、@luojiyin1987（ü 韵母写法、查询去重）、@broxlighters（会话隔离与私密记录）、@9tong（问字开关、中文优先、系统文本替换、打包脚本）、@Uiye4096（自定义短语、默认标点、半角问号修复）、@Tsuk1ha（键盘布局、- = 翻页、词库导入）、@lll114514lll1919810lll（中英混杂词）

## 0.1.2 · 2026-09-12 · beta

- 随包带一个本地整句模型（约 56 MB，全程离线）：敲完一段拼音停一下，整句候选换成模型认为更通顺的那句；会看光标前的几十个字，同一段拼音在不同上下文里给的句子可能不同；密码框里不读上下文
- 「偏好设置 → 云服务」新增「本地整句模型」开关，关掉只用词库统计，与云联想互不影响
- 基础词库新增 5398 条常整块打出的组合（我的 / 好的 / 不知道）与 48 条常用领域词（对齐 / 后端 / 词库…），整句转换的语言模型一并更新
- 输入日志升级到 v1，多记退格重打、直通字符、云端联想、上文断开等事件，仍只写本机、随时可关可清

## 0.1.0-alpha.2 · 2026-09-12 · alpha

- 随包带一个本地整句模型（约 56 MB，全程离线，CPU 推理几十毫秒）：敲完一段拼音停一下，整句候选换成模型认为更通顺的那句；会读应用里光标前的几十个字当上下文（记事本 / 浏览器 / 终端都支持），「设置 → 云服务」可关闭
- 密码框按 Windows 输入法规范处理：应用禁用了文本服务的密码框里所有按键直接放行、不组句（与 macOS 的 Secure Input 一致）；应用声明为私密的输入框里不读上下文、不学习、不记输入日志、不发云端
- 基础词库新增 5398 条常整块打出的组合与 48 条常用领域词，整句转换的语言模型一并更新
- 设置程序里的改动即时生效这条更可靠了：之前输入法在多个应用里同时开着时设置可能要等重启才生效
- 输入法日志按天分文件、只保留 7 天
- **已知问题：内测版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡**

## 0.1.0-alpha.1 · 2026-09-12 · alpha

- Windows 版首个内测版，功能与 macOS 版一致；安装包一键安装，装完即可在输入法列表中选择「青简」，升级时直接安装新版
- 单击 Shift 切换中 / 英，任务栏与桌面上可拖动的悬浮状态条都显示当前模式；状态条上可切换全角 / 半角标点，中英各记一份
- 候选窗口跟随光标，支持深色模式与横排 / 竖排，拼音行带内联下划线
- 整句输入、简拼、拼写纠错、模糊音、双拼、英文模式补全、中英混输、emoji、日期时间与算式、按码点输入字符
- 候选旁的译词与生词标记：Ctrl + 数字直接输出译词，Shift + 数字删除候选
- 可选的云联想：云端候选、整句补全、问字、Ctrl + Alt + T 翻译选中的文字
- 设置程序（开始菜单「青简设置」）与 macOS 偏好设置页面一致，修改即时生效
- **已知问题：内测版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡**

## 0.1.1 · 2026-09-08 · beta

- 中英混输：一句拼音的末尾接英文词直接成句，`woxiangxuehaorust` → 我想学好rust，`wodeid` → 我的ID；像 `database` 这种也能读成拼音的按哪种更通顺定
- 组句编辑：⌥⌫ 删一个音节、⌘⌫ 删到开头、⌥← / ⌥→ 按音节移动光标
- 组句中敲半角标点进入英文直输段，中文模式下也能直接打 `hello, world?`
- 学习更稳：退格撤销能回看最近四次上屏；一段拼音分几次选完的，合起来两次就成词；云端词按敲的读音学，不再学进错读音
- `key` 这类末尾落单一个字母的输入，选惯了中文词就中文排前面
- 菜单栏「中 / 英」状态项不再跳回最左边

## 0.1.0 · 2026-09-07 · beta

- 整句输入：一口气敲完一句再上屏，简拼也行；敲错一两个字母会按最像的读法给出候选
- 候选旁有词性和译词，英语 / 日语可选，日语带假名；⌥ + 数字直接打出译词；没见过几次的译词画橙色
- 模糊音、双拼（小鹤 / 自然码 / 微软 / 搜狗）、英文模式下的补全与拼错纠正、中英混输、emoji、日期时间与算式快捷输入
- 越用越顺：记住你选过的词和常连着打的词，自动造词，选错了退格重选会把学到的撤回
- 可选的云联想：云端候选词、整句补全、问字、翻译选中文字、缺少的译词自动补；缺省关闭，偏好设置里可测试连接
- 基础词库 8.7 万条，另有 11 本领域词库随包；可导入自己的词库（青简 TSV、Rime、.qj）
- 偏好设置分九页，含输入统计（今天 / 最近 7 天 / 累计打了多少字，学习语言的词汇量）
- 输入法崩溃也不丢学习数据、不丢正在打的字；安装包装完自动注册输入法
